# Downloaded AV1 CPU video archive

Download every `downloaded_video_av1_cpu.7z.*` file into the same folder, then extract only the first file:

```sh
7z x downloaded_video_av1_cpu.7z.001
```

7-Zip will automatically read `.002`, `.003`, and the remaining volumes to restore `downloaded_video_av1_cpu.mp4`.

Original video SHA-256:

```text
621e8b53fca331193c6f3a35fc5cda9f83833189e5d1c85dd32d096c30600b05
```
